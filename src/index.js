// import Carousel from './components/Carousel';
import './../static/sass/main.scss';

async function init() {
  const $ = document.querySelector.bind(document);
  const $$ = document.querySelectorAll.bind(document);

  

  // $('.carousel-container').innerHTML = Carousel();
}

init();

